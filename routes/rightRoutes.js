const express = require('express');
const router = express.Router();
const rightController = require('../controllers/rightController');

/*
router.get('/', rightController.getAllRights);
router.post('/', rightController.createRight);
router.get('/:rightId', rightController.getRightById);
router.put('/:rightId', rightController.updateRight);
router.delete('/:rightId', rightController.deleteRight);
*/

module.exports = router;